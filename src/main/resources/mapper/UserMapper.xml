<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.back.mapper.UserMapper">
    <update id="SetAdminById" >
        update admin set name = #{name}, phone = #{phone}, email = #{email}
        where id = #{id}
    </update>
    <update id="SetAdminPassageById" >
        update admin set password = #{password}
        where id = #{id}
    </update>

</mapper>
